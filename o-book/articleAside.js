// 右侧边的初始化逻辑
define(async (load) => {
    const asideEle = $("#articleAside");

    if (!asideEle) {
        console.warn('article aside no exits');
        return;
    }

    return {
        initAside() { 
            
        }
    };
});